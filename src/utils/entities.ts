/**
 * This file contains some hard coded entities
 * Like some fixed emoji, media, etc
 * With only the minimum needed information to use them
 * like IDs and Name to mention emoji and people
 */

import { Emoji } from "discord.js";

export const emojis = {
  TB: new Emoji(null, { id: "899852654832930847", name: "tb" }),
};
